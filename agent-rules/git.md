# Git and Pull Requests

Use concise Conventional Commit messages, such as `feat(channels): add binding
status`. Keep commits focused. Pull requests must state intent and risk, link
relevant work, list validation, identify affected routes and roles, include
screenshots for visible UI changes, and preserve unrelated worktree changes.
