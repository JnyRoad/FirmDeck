declare function t(id: string): string;

/** Keep declaration-only compatibility examples outside production diagnostics. */
export declare const ignoredTypeFixture: string = t('Declaration-only product prose');
